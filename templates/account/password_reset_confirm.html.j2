{% extends 'base.html.j2' %}

{% block content %}
  <div class="container">
    {% if validlink %}
    <div class="page-header">
      <h3>비밀번호 재설정하기</h3>
    </div>
    <form method="post">
      {% csrf_token %}
      {% for field in form %}
        <div class="form-group">
          <label for="cname" class="col-sm-2 control-label">{{ field.label_tag }}</label>
          <div class="col-sm-10">
            {% if field.errors %}
                <div class="alert alert-danger alert-dismissable">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                    {{ field.errors }}
                </div>
            {% endif %}
            {{ field }}
            <br />
            {{ field.help_text }}
          </div>
        </div>
      {% endfor %}
      <div class="form-group last">
        <div class="col-lg-offset-2 col-lg-10">
            <button type="submit" class="btn btn-primary">재설정하기</button>
            <a href="{% url 'index' %}">
                <button type="button" class="btn btn-warning">돌아가기</button>
            </a>
        </div>
      </div>
    </form>
    {% else %}
      <p>
        잘못된 비밀번호 재설정 접근입니다. 이미 사용되었거나 만료된 링크입니다. 다시한번 비밀번호 재설정 요청을 해주세요.
      </p>
    {% endif %}
  </div>
{% endblock %}
