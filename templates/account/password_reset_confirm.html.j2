{% extends 'base.html.j2' %}

{% block content %}
<div class="container">
  <div class="page-header">
    <h1>비밀번호 재설정</h1>
  </div>
    {{ form.as_p }}
  <form class="cmxform form-horizontal style-form" method="post" enctype="multipart/form-data" action="">
    {% csrf_token %}
    {% if form.errors %}
    <div class="alert alert-danger alert-dismissable">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        {{ form.errors }}
    </div>
    {% endif %}
    <div class="form-group ">
        <label for="cname" class="control-label col-lg-2"> <label for="id_new_password1">새 비밀번호:</label> </label>
        <div class="col-lg-10">
            <input type="password" name="new_password1" required id="id_new_password1" class="form-control">
            <br />
            <ul><li>다른 개인정보와 비슷한 비밀번호는 사용할 수 없습니다.</li><li>비밀번호는 최소 8자 이상이어야 합니다.</li><li>비밀번호는 일상적으로 사용되는 비밀번호일 수 없습니다.</li><li>비밀번호는 전부 숫자로 할 수 없습니다.</li></ul>
        </div>
    </div>
    <div class="form-group ">
        <label for="cname" class="control-label col-lg-2"> <label for="id_new_password2">새 비밀번호 (확인):</label> </label>
        <div class="col-lg-10">
            <input type="password" name="new_password2" required id="id_new_password2" class="form-control">
            <br />

        </div>
    </div>
    <div class="form-group last">
        <div class="col-lg-offset-2 col-lg-10">
            <button type="submit" class="btn btn-primary">변경하기</button>
        </div>
    </div>
</form>
</div>

{% endblock %}
